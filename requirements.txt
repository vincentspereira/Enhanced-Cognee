# Enhanced Cognee Requirements
# Enterprise-grade memory stack dependencies with Enhanced features

# Enhanced Stack Database Dependencies
psycopg2-binary==2.9.12            # PostgreSQL adapter (replaces SQLite)
pgvector==0.4.2                    # Vector support for PostgreSQL
qdrant-client==1.18.0             # Qdrant vector database client (replaces LanceDB)
neo4j==5.25.0                      # Neo4j graph database client (replaces Kuzu)
redis==5.1.1                       # Redis client (new cache layer)

# Web Framework and API
fastapi==0.136.1                   # Web framework
uvicorn==0.47.0                    # ASGI server
pydantic==2.13.4                    # Data validation
pydantic-settings==2.14.1           # Settings management

# Database and ORM
sqlalchemy==2.0.49                 # SQL ORM
alembic==1.18.4                    # Database migrations
asyncpg==0.31.0                    # Async PostgreSQL driver

# Authentication and Security
python-jose[cryptography]==3.5.0   # JWT handling
passlib[bcrypt]==1.7.4             # Password hashing
python-multipart==0.0.29            # Form data parsing
cryptography==42.0.5                # Cryptographic operations

# HTTP and Networking
httpx==0.28.1                      # Async HTTP client
aiofiles==23.2.1                   # Async file operations
requests==2.34.2                   # HTTP requests (for some integrations)

# Configuration and Environment
python-dotenv==1.2.2               # Environment variables
python-decouple==3.8               # Configuration management
pyyaml==6.0.3                      # YAML parsing

# Logging and Monitoring
structlog==24.1.0                  # Structured logging
rich>=13.8.0,<14.0                 # Rich console output (must be >=13.8 for typer 0.25.x)
prometheus-client==0.25.0          # Prometheus metrics
opentelemetry-api==1.41.1          # OpenTelemetry tracing
opentelemetry-sdk==1.41.1

# Data Processing and Analysis
numpy==1.26.4                      # Numerical computing
pandas==2.2.1                      # Data analysis
scikit-learn==1.7.2                # Machine learning
scipy==1.15.3                      # Scientific computing

# Multi-language Support (Sprint 9)
langdetect==1.0.9                   # Language detection (28 languages)

# Text Processing and NLP
tiktoken==0.13.0                    # Token counting
nltk==3.9.4                        # Natural language processing
spacy>=3.8.13,<3.9                 # NLP pipeline (3.8.14 doesn't exist on PyPI; 3.8.13 is latest in the 3.8.x line)
textstat==0.7.13                    # Text statistics

# Machine Learning and AI
transformers==4.40.1               # HuggingFace transformers
torch==2.12.0                       # PyTorch
sentence-transformers==2.6.1       # Sentence embeddings
accelerate==1.13.0                 # PyTorch acceleration
datasets==2.18.0                   # Dataset handling

# AI Provider SDKs
openai>=1.80.1,<2.0                # OpenAI API (pyproject requires >=1.80.1)
anthropic==0.102.0                   # Anthropic API
google-generativeai==0.8.6         # Google AI
mcp>=1.0.0                         # Model Context Protocol SDK (FastMCP server for MCP tools)

# AWS SDK (imported at module level by src/security/enhanced_security_framework.py
# for credential rotation tooling; required even if AWS features aren't used)
boto3>=1.34,<2.0
botocore>=1.34,<2.0

# Rate-limiting for FastAPI (imported at module level by enhanced_security_framework.py)
slowapi>=0.1.9,<1.0

# More module-level imports found by clean-CI install audit (each one fails
# test-collection at import time because they are imported unconditionally):
PyJWT>=2.8,<3.0                    # `import jwt` in src/security/{auth,enhanced_security_framework}.py
bleach>=6.0,<7.0                   # HTML/XSS sanitization
dataclasses-json>=0.6,<1.0         # JSON (de)serialization for @dataclass
feedparser>=6.0,<7.0               # RSS/Atom feed parsing (used by ingest_url)
plotly>=5.18,<7.0                  # Charting (knowledge-graph viz endpoints)
markdown>=3.5,<4.0                 # Markdown -> HTML rendering

# Memory and Caching
redis==5.1.1                       # Redis client
hiredis==2.3.2                     # Redis parser
diskcache==5.6.3                   # Disk-based caching

# Task Queue and Background Jobs
celery==5.6.3                      # Distributed task queue
kombu==5.6.2                       # messaging library

# Template Engine
jinja2==3.1.6                      # Template rendering
markupsafe==2.1.5                  # Secure markup

# CLI and Utilities
typer==0.25.1                       # CLI framework
click==8.4.0                       # Command line interface
# rich-cli removed: it's a phantom dependency -- never imported in our code,
# and the pinned version 4.3.0 doesn't exist on PyPI (max published is 1.8.1).
# The `rich` package (transitive via typer) provides everything we need.

# File Processing
python-magic==0.4.27               # File type detection
pillow==10.2.0                     # Image processing
python-docx==1.2.0                 # Word document processing
openpyxl==3.1.5                    # Excel file processing
pdfplumber==0.11.9                 # PDF processing

# Data Validation and Serialization
marshmallow==3.21.2                # Object serialization
cerberus==1.3.8                    # Data validation

# Graph Processing (Neo4j integration)
neo4j==5.25.0                      # Neo4j official Python driver
py2neo==2021.2.4                   # Alternative Neo4j client
networkx==3.4.2                    # Network analysis

# Vector Operations
faiss-cpu==1.13.2                   # Vector similarity search (alternative)
annoy==1.17.3                      # Approximate nearest neighbors
hnswlib==0.8.0                     # Hierarchical navigable small world

# Async Support
asyncio==3.4.3                     # Async programming
asyncio-mqtt==0.16.2               # MQTT async client
aiohttp==3.13.5                     # Async HTTP client/server

# Testing (development only)
pytest>=8.1.0,<9.0                 # Testing framework (pinned to 8.x; 9.x removes APIs we use)
pytest-asyncio>=0.24.0,<2.0        # Async testing (0.24 added pytest 8 support; 1.x is current)
pytest-mock>=3.14,<4.0             # Mocking in tests
pytest-cov>=5.0,<8.0               # Coverage reporting (5+ supports pytest 8)

# Development Tools
black==24.3.0                      # Code formatting
flake8==7.0.0                      # Linting
mypy==1.9.0                        # Type checking
pre-commit==3.6.0                  # Git hooks

# Documentation
sphinx==8.1.3                      # Documentation generation
sphinx-rtd-theme==3.1.0            # ReadTheDocs theme

# Utilities
python-dateutil==2.9.0.post0            # Date utilities
pytz==2024.1                       # Timezone handling
schedule==1.2.2                    # Task scheduling
watchdog==3.0.0                    # File system watching

# Error Handling and Monitoring
sentry-sdk==1.43.0                 # Error monitoring
rollbar==1.3.0                     # Error tracking
# notifier removed: phantom dependency -- never imported, and the pinned
# version 1.3.3 does not exist on PyPI (max published is 1.0.3). This was
# breaking CI dependency resolution. If notification batching is needed,
# we already have the Slack/Discord/email channels in src/notifications/.

# Performance Optimization
numba==0.65.1                      # JIT compiler
cython==3.2.4                     # C extensions
orjson==3.11.9                     # Fast JSON

# Development Environment
ipython==8.22.2                    # Enhanced Python REPL
jupyter==1.1.1                      # Jupyter notebook support